import * as actionTypes from './constants'
const defaultstate = {

}

const reducer = (state = defaultstate, action) => {
    switch (action.type) {
        default:
            return state;
    }
}

export default reducer